@model DateTime?

@Html.TextBoxFor(m => m.HasValue ? (Convert.ToDateTime(m.Value)!=DateTime.MinValue? m.Value.ToString(CMS_i18n.Formats.DateFormat) : "") : "", new { @class = "datefield", type = "date"  })
